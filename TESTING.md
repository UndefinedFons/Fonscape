# 测试指南

Fonscape 使用 Node.js 内置测试运行器承担单元和集成测试，并使用 Playwright 保护浏览器中的评论关键路径。测试面向主题的公开契约：内容解析、路由、权限、数据库一致性、部署适配、可访问性和性能预算。

## 测试分层

| 分层 | 保护对象 | 优先写法 |
| --- | --- | --- |
| 单元测试 | 纯解析、格式化、路由和工具函数 | 直接调用函数，使用小型固定数据 |
| 集成测试 | API 处理器、权限、迁移和适配器 | 内存 SQLite 数据库，显式构造请求上下文 |
| 渲染测试 | 可访问的组件行为和用户可见状态 | React 渲染后断言行为结果 |
| E2E 测试 | 真实浏览器中的评论关键路径 | 一条聚焦的 Playwright 流程 |
| 构建检查 | 生成目标、缓存规则和性能预算 | 确定性生成结果检查 |

## 新增测试

当行为保护用户可见契约、安全边界、数据完整性，或高风险回归难以通过类型检查和 Lint 发现时，才添加长期测试。行为属于同一契约时，优先扩展现有测试文件。

新功能优先覆盖主成功路径和主要失败路径。缺陷修复只添加修复前失败、修复后通过的最小测试。能用一个参数化用例表达时，不要为每个内部分支复制独立测试。

## 判定测试是否有效

一条测试只有在被保护的行为失效时失败，才算有效。新增或改写用例后，用故障注入或等价方法确认这一点，再恢复代码。排序、去重和筛选使用打乱顺序、含重复值的输入，让“原样返回”无法通过。

## 质量规则

- 测试可观察行为，不测试实现文本。读取源码的测试必须有明确的高风险理由。
- 同一行为只在最合适的一层保护，不在单元、集成和 E2E 中重复断言。
- 优先复用固定数据和 helper，不复制数据库、请求和用户初始化。
- 保持失败可定位。一个测试对应一个契约；失败名称不能指向问题时，应拆分或重命名。测试名称描述当前行为，不保留历史版本号。
- 测试交互和动画时保持可访问性与 `prefers-reduced-motion` 行为。
- 保持单元和集成测试确定性。依赖时间的测试应使用注入的时钟和计时器。

## 删除测试

出现以下情况时删除或合并测试：

- 被测功能已经不存在。
- 同一契约已在更合适的层级得到保护。
- 兼容承诺到期，且对应实现也一并删除。
- 测试只固定源码文本、内部 DOM 形状，或不会改变用户可见行为的排版细节。
- 类型检查、Schema 校验或运行时校验已经提供同等保护，并给出更清晰的错误信息。

不要因为某个测试曾经发现过缺陷就永久保留。保留的条件是：缺陷仍可能发生，且该测试是最清楚的保护方式。功能删除时，同步删除对应测试、固定数据和 helper。

## 审查节奏

开发新功能时，先检查相邻测试是否已有重叠，再决定是否新增文件。版本审查时，确认新测试仍对应受支持的契约，已移除的兼容代码没有留下孤立固定数据。覆盖率与测试数量仅作参考，不作为目标。

如果常规本地测试在没有扩大覆盖范围的情况下明显变慢，应审查重复迁移初始化、浏览器重测试，以及重复同一集成路径的测试。
